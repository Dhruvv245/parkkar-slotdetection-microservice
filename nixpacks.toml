[phases.setup]
nixPkgs = ['python311', 'libGL', 'libglib', 'glib', 'opencv4']

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.build]
cmds = ['echo "Build completed"']

[start]
cmd = 'uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 1'
